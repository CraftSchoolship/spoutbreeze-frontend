import PricingPage from "@/components/pricing/PricingPage";

export const metadata = {
  title: "Pricing - BlueScale",
  description: "Choose the plan that fits your streaming needs.",
};

export default function Pricing() {
  return <PricingPage />;
}
